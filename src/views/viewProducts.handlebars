<div class="infoProd" >
    <h1>Listado de Productos con handlebars </h1>
    <div class="detalleProd">
        <div class="product-container">
            {{#each products}}
            <div class="product">
                <h2>Descripcion {{thisdescription}} </h2>
                <p>Precio ${{this.price}} </p>
                <a href="/api/products/{{this._id}} ">Ver Detalle </a>
            </div>
            {{/each}}
        </div>
    </div>
</div>